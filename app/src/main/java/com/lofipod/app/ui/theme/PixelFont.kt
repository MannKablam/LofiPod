package com.lofipod.app.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.lofipod.app.R

/**
 * Press Start 2P — chunky 8-bit pixel font from Google Fonts (OFL license,
 * see assets/PressStart2P-OFL.txt). Used for the "LofiPod" wordmark.
 *
 * This font has tall, blocky pixel glyphs — pair with smaller font sizes
 * (12–16 sp) so it doesn't dominate the layout.
 */
val PressStart2P: FontFamily = FontFamily(Font(R.font.press_start_2p))
