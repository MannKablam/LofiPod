<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- No FOREGROUND_SERVICE_DATA_SYNC: the v0.6.9 download rewrite ditched
         Media3's DownloadService entirely. Downloads now run on the app
         process while the PlaybackService (mediaPlayback type) keeps it
         alive during playback. -->

    <!-- API 33+: needed to post the media notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- In-app updater: needed to invoke the system installer for an APK
         downloaded from GitHub releases. The user still has to grant the
         per-app "Install unknown apps" permission once via system Settings;
         the app detects that and prompts. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Lets the app launch ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS to
         prompt the user to mark LofiPod as "Unrestricted." Doze and App
         Standby can throttle background CPU/scheduling even with a
         mediaPlayback foreground service, causing playback jitter at long
         sessions or >1x speed. Without this permission the system rejects
         the intent with SecurityException. The user still has to confirm
         the prompt — declaring this permission only enables the request,
         not the exemption itself. Surfaced in Settings -> System. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".LofiPodApp"
        android:label="LofiPod"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.LofiPod"
        android:supportsRtl="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode"
            android:theme="@style/Theme.LofiPod">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".player.PlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- FileProvider for sharing the downloaded APK URI with the system
             installer. Direct file:// URIs are blocked since Android 7;
             FileProvider hands out a content:// URI the installer can read. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>
</manifest>
